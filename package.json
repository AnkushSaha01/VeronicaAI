{
  "name": "veronica-ai-monolith",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install --legacy-peer-deps && npm run build && cd .. && node -e \"const fs=require('fs'); fs.mkdirSync('public', { recursive: true }); fs.cpSync('frontend/out', 'public', { recursive: true }); fs.mkdirSync('backend/public', { recursive: true }); fs.cpSync('frontend/out', 'backend/public', { recursive: true });\" && cd backend && npm install --legacy-peer-deps"
  }
}
